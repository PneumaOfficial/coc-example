  #include <aws/core/Aws.h>
  #include <lua.hpp>

  #include <Framework.h>

  int main(int numArgs, char* args[]) {
          lua_State* lua;
          // create a web socket connection


          // Create a web socket connection

          return 0;
  }
